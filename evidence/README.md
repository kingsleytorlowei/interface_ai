# Evidence

One directory per run. Each holds a redacted `events.jsonl` (append-only, one event per line),
`result.json` (the typed run result), and `snapshots/` (redacted tree + text, and a screenshot)
wherever the engine stopped, asked for approval, or handed off. In events and results, the
run's sensitive inputs and outputs appear only as placeholders such as `«inputs.member_id»`.
Snapshots are scrubbed of those same values, but show whatever else was on screen (e.g. a
member name the capability never extracted); all data here is synthetic. Discovery runs also
keep `transcript.json` (the model's reasoning and tool calls) and `capability.draft.json`.
Since the sub-account fix, transcripts replace each screen the model saw with that
observation's digest in `events.jsonl`; the two earlier lookup discoveries predate it and
still include the screens (their undeclared data was redacted only because the lookup
extracted the member's name as an output).

## Recorded runs (no LLM, no human)

Every run a script can reproduce, re-recorded from scratch with
`uv run python scripts/record_evidence.py` (starts its own mock banks; exits 1 if any run
doesn't give its expected result). Each directory also has `capability.json` (the exact
artifact replayed) and `scenario.json` (tenant, injected faults, artifact source).

<!-- recorded:start -->
_Generated by `uv run python scripts/record_evidence.py`; do not edit by hand._

| Scenario | Artifact | Result | Run |
|---|---|---|---|
| Happy path: known member | `corebank.member.lookup_balance@0.1.0` (discovered) | `success`; committed: none | [01-happy-path](recorded/01-happy-path/) |
| Business outcome: member not found | `corebank.member.lookup_balance@0.1.0` (discovered) | `business_outcome` no_results; committed: none | [02-business-outcome](recorded/02-business-outcome/) |
| Fault recovery: session expired mid-run | `corebank.member.lookup_balance@0.1.0` (discovered) | `success`; recoveries: signed_out→reauthenticate; committed: none | [03a-recovery-session-expired](recorded/03a-recovery-session-expired/) |
| Fault recovery: transient 503 | `corebank.member.lookup_balance@0.1.0` (discovered) | `success`; recoveries: service_unavailable→retry; committed: none | [03b-recovery-transient-503](recorded/03b-recovery-transient-503/) |
| Fault recovery: broadcast notice interstitial | `corebank.member.lookup_balance@0.1.0` (discovered) | `success`; recoveries: system_notice→click; committed: none | [03c-recovery-broadcast-notice](recorded/03c-recovery-broadcast-notice/) |
| Fatal failure: core system error on search | `corebank.member.lookup_balance@0.1.0` (discovered) | `failure` app_error at `click_search_button`; committed: none | [04-fatal-failure](recorded/04-fatal-failure/) |
| Tenant drift: pinnacle-recorded lookup on riverbend | `corebank.member.lookup_balance@0.1.0` (discovered) | `failure` target_not_found at `click_search_button`; drift: member_id_field[1]; committed: none | [05-riverbend-drift](recorded/05-riverbend-drift/) |
| Tenant overlay: the same lookup on riverbend | `corebank.member.lookup_balance@0.1.0+riverbend` (discovered + riverbend overlay (hand-written, verified)) | `success`; committed: none | [05b-riverbend-overlay](recorded/05b-riverbend-overlay/) |
| Tenant overlay: the sub-account form on riverbend | `corebank.subaccount.prepare@0.1.0+riverbend` (discovered + riverbend overlay (hand-written, verified)) | `success`; committed: ['click_continue_button'] | [05c-riverbend-overlay-form](recorded/05c-riverbend-overlay-form/) |
| Approval rejected: unattended irreversible step | `corebank.subaccount.open@1.0.0` (fixture (hand-written)) | `aborted` at `confirm`; committed: none | [06-approval-rejected](recorded/06-approval-rejected/) |
| Fault recovery: one page load hangs past the budget | `corebank.member.lookup_balance@0.1.0` (discovered) | `success`; recoveries: timeout→retry; committed: none | [07a-recovery-hung-load](recorded/07a-recovery-hung-load/) |
| Timeout: the app stays slower than the budget | `corebank.member.lookup_balance@0.1.0` (discovered) | `failure` timeout at `entry`; recoveries: timeout→retry; committed: none | [07b-timeout](recorded/07b-timeout/) |
| Form flow: sub-account prepared up to the review screen | `corebank.subaccount.prepare@0.1.0` (discovered) | `success`; committed: ['click_continue_button'] | [08a-subaccount-prepare](recorded/08a-subaccount-prepare/) |
| Business outcome: deposit below the minimum | `corebank.subaccount.prepare@0.1.0` (discovered) | `business_outcome` subaccount_values_rejected; committed: ['click_continue_button'] | [08b-subaccount-rejected](recorded/08b-subaccount-rejected/) |
<!-- recorded:end -->

## Stability measurements

`uv run cua stability <capability> [--tenant riverbend] --params ... --params ...` replays an
approved capability ten times unattended, cycling through the input sets, and writes each
run under `stability/<capability>@<version>-<timestamp>/`. The report (verdict, success rate,
consistency, drift, recoveries, durations, and each run's id) sits next to what it measured
in `catalog/`, and clears the capability for unattended replay only if it meets the app's
`unattended` thresholds.

| Capability | Tenant | Report | Runs |
|---|---|---|---|
| `corebank.member.lookup_balance@0.1.0` | pinnacle | `stable`: 10/10, no drift, no recoveries, p50 2.82 s | [stability/…@0.1.0-20260924T164714Z](stability/corebank.member.lookup_balance@0.1.0-20260924T164714Z/) |
| `corebank.subaccount.prepare@0.1.0` | pinnacle | `stable`: 10/10, no drift, no recoveries, p50 4.43 s | [stability/…@0.1.0-20260924T164748Z](stability/corebank.subaccount.prepare@0.1.0-20260924T164748Z/) |
| `corebank.member.lookup_balance@0.1.0+riverbend` | riverbend | `stable`: 10/10, no drift, no recoveries, p50 2.83 s | [stability/…@0.1.0+riverbend-20260924T164844Z](stability/corebank.member.lookup_balance@0.1.0+riverbend-20260924T164844Z/) |
| `corebank.subaccount.prepare@0.1.0+riverbend` | riverbend | `stable`: 10/10, no drift, no recoveries, p50 4.5 s | [stability/…@0.1.0+riverbend-20260924T164917Z](stability/corebank.subaccount.prepare@0.1.0+riverbend-20260924T164917Z/) |

The mock bank is deterministic, so every real measurement here is 10/10. That the gate
refuses flaky, broken, drifting, too-short and stale measurements is shown by the tests
(`tests/test_stability.py`, `tests/test_cli.py`), not by these runs.

## Manual runs

Runs that need a model key or a human, recorded once and committed as-is.

| Scenario | Command | Result | Run |
|---|---|---|---|
| Real LLM discovery, attempt 1 (`claude-opus-5`) | `uv run cua discover goals/corebank.member.lookup_balance.yaml --headed` | recorded in 5 turns, but mapped outcomes onto happy-path screens (`system_error` → `member_search`) | [20260923T191821Z-91a1a6](20260923T191821Z-91a1a6/) |
| Verification of attempt 1 | (same command, second phase) | `business_outcome` `system_error` on the search form: **the gate caught a bad draft**; approval refused. Led to the schema rule "outcomes can't sit on the success path". | [20260923T191921Z-0beaf1](20260923T191921Z-0beaf1/) |
| Real LLM discovery, attempt 2 (after the fix) | (same command) | recorded in 5 turns; 10 input / 21,472 cache-read / 8,576 cache-write / 1,284 output tokens (~$0.10) | [20260923T192440Z-4e94fc](20260923T192440Z-4e94fc/) |
| Verification of attempt 2 | (same command, second phase) | `success`; approved as `0.1.0` with `click_search_button` lowered to read_only | [20260923T192509Z-abb84b](20260923T192509Z-abb84b/) |
| Real LLM discovery, lookup, attempt 3 (stability check, after the two-member verification) | `uv run cua discover goals/corebank.member.lookup_balance.yaml` | recorded in 5 turns; 10 input / 20,472 cache-read / 8,314 cache-write / 918 output tokens. Verified on members 12345 and 45678, nothing pruned. **Structurally identical to the approved `0.1.0`** (same targets, strategies, steps, checkpoints, outcome states, success condition; only wording differs). The draft was not kept. | [20260924T152005Z-fa0e6f](20260924T152005Z-fa0e6f/); verifications [20260924T152054Z-95e6e0](20260924T152054Z-95e6e0/), [20260924T152057Z-2347e0](20260924T152057Z-2347e0/) |
| Real LLM discovery, sub-account form flow, attempt 1 (`claude-opus-5`) | `uv run cua discover goals/corebank.subaccount.prepare.yaml` | recorded in 9 turns and verified, but a fallback strategy for *Continue* was anchored on the heading `OPEN SUB-ACCOUNT — <member name>`: **data in the artifact**, invisible to value-based redaction because the name was never an input or output. Not approved; the draft was deleted. Led to verification with a second member, per-strategy audit and pruning, a sensitive-value check on save, and screen trees removed from saved transcripts. | [20260924T144939Z-0675f7](20260924T144939Z-0675f7/) |
| Verification of attempt 1 | (same command, second phase) | `success` (one replay, before the fix) | [20260924T145038Z-e01640](20260924T145038Z-e01640/) |
| Real LLM discovery, sub-account form flow, attempt 2 (after the fix) | (same command) | recorded in 9 turns; 18 input / 59,866 cache-read / 12,646 cache-write / 1,712 output tokens. The model never tried *Confirm*. `capability.draft.json` and `events.jsonl` here still show the dropped anchor: they record the draft as synthesized, before verification pruned it. | [20260924T151259Z-435f59](20260924T151259Z-435f59/) |
| Verification of attempt 2, members 12345 and 45678 | (same command, second phase) | both `success`; the strategy audit dropped *Continue*'s name-anchored fallback, leaving the artifact free of member data; approved as `0.1.0` with the search and *Open Sub-Account* clicks lowered to read_only | [20260924T151343Z-72dbea](20260924T151343Z-72dbea/), [20260924T151348Z-a49d63](20260924T151348Z-a49d63/) |
| Tenant overlay verification: lookup on riverbend, members 12345 and 45678 | `uv run cua verify-overlay corebank.member.lookup_balance --tenant riverbend --base-url http://127.0.0.1:8012 --params '{"member_id": "12345"}' --params '{"member_id": "45678"}'` | both `success` as `0.1.0+riverbend`, no drift, nothing pruned; the hand-written overlay (4 targets, from `recorded/05`) became approvable | [20260924T162632Z-cae514](20260924T162632Z-cae514/), [20260924T162636Z-691055](20260924T162636Z-691055/) |
| Tenant overlay verification: sub-account form on riverbend | `uv run cua verify-overlay corebank.subaccount.prepare --tenant riverbend ...` (two input sets, members 12345 and 45678) | both `success` as `0.1.0+riverbend`, no drift; 2 of 8 targets overridden | [20260924T162639Z-5e3042](20260924T162639Z-5e3042/), [20260924T162644Z-2d34ed](20260924T162644Z-2d34ed/) |
| Console handoff: member alert | `uv run cua replay corebank.member.lookup_balance --params '{"member_id": "23456"}' --operator console` | `success` after a human handoff: escalated on `member_alert` at `click_search_button`; operator `kingsley` clicked *Acknowledge* in the live session (captured as a human action) and handed back; engine resumed and read the outputs; nothing committed | [20260923T194339Z-f6c82a](20260923T194339Z-f6c82a/) |

Console screenshots (same run):
1. [The operator console with the pending handoff](console/1-console-handoff-pending.png): reason, run id, step, and the session's own snapshot.
2. [The live session the operator works in](console/2-live-session-member-alert.png): the headed teller window on the MEMBER ALERT screen, just before *Acknowledge*.
